import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mainContainer: {
    flexDirection: "column",
    alignItems: "center",
    marginTop: 50,
  },

  container: {
    flexDirection: "row",
    alignItems: "center",
  },
  text: {
    fontSize: 50,
  },

  subText: {
    fontSize: 20,
    color: "#FFAD87",
  },
  img: {
    width: 40,
    height: 40,
  },
});
